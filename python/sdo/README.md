# SDO – Tests (statische Outlier-Detection)

Von **Projektroot** aus:

- **Alle Tests:**  
  `python python/sdo/test_sdo.py`
- **Nur bestimmte Tests (z.B. 1, 2, 4):**  
  `python python/sdo/test_sdo.py --test 1 2 4`

Voraussetzung: `maturin develop` im Projektroot. Optional: `scikit-learn`.
